name: Selective Tests with Tuist (Modular)

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  pull-requests: write
  checks: write
  contents: read
  statuses: write

jobs:
  selective-tests:
    runs-on: macos-latest
    timeout-minutes: 20

    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Tuist
        run: |
          brew install tuist

      - name: Run Selective Tests with Tuist
        id: tuist-test
        run: |
          echo "Running selective tests with Tuist..."
          tuist test manual_Trigger \
            --platform macOS \
            --selective-testing \
            -- CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO
        continue-on-error: true

      - name: Check test results
        run: |
          echo "=========================================="
          echo "Selective Test Results"
          echo "=========================================="
          echo ""
          echo "Tuist Selective Testing: ${{ steps.tuist-test.outcome }}"
          echo ""
          echo "=========================================="

          if [ "${{ steps.tuist-test.outcome }}" == "failure" ]; then
            echo ""
            echo "❌ Tests failed!"
            echo ""
            echo "💡 Check the logs above for detailed error messages"
            echo "💡 Tuist ran only the tests affected by your changes"
            echo ""
            exit 1
          else
            echo ""
            echo "✅ All affected tests passed!"
            echo "💡 Tuist used selective testing to run only relevant tests"
            echo ""
          fi
